#include <bits/stdc++.h>
using namespace std;

int main() {
	int n;
	while(cin >> n) {
		if( n == 0) return 0;
		int index = 0;
		for (int i = 0; i < n; i++) {
			int count;
			cin >> count;
			for (int j = 0; j < count; j++) {

			}
		}
	}
	return 0;
}
